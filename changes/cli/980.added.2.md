Added a function-only `param_dict` argument to `job-scripts create` and `create-locally` (hidden from the CLI help), so programmatic callers can supply template parameters as a dictionary instead of a JSON parameter file
