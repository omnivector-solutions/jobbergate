Renamed configuration field `ARMADA_API_BASE` to `BASE_API_URL` for consistency across the codebase. The previous field name was kept for backward compatibility, but it is recommended to update to the new field name in future configurations
